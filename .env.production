# Production build-time config (committed — public, non-secret).
# Vite inlines VITE_* vars into the bundle during `vite build` (production mode).
# Points Maree's runtime token sync (useTokenSync) at the Tessor design system on Cloudflare Workers.
VITE_WORKBENCH_URL=https://tessor.business-eb4.workers.dev
