---
description: Post-merge sync — switch to main, pull, delete feature branch
---

Sync up after the PR has been merged:

1. Switch back to `main`.
2. Pull the latest.
3. Delete the local feature branch (the one we just merged).
4. Confirm everything is clean — show `git status` and current branch.
5. Give me a one-line summary of what shipped in this session.
